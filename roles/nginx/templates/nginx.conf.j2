server {
        listen 80 default_server;
        listen [::]:80;
        root {{ custom_directory }};
        index index.html;
        location / {
                try_files $uri $uri/ =404;
        }
}
